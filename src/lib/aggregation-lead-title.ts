// Aggregation land page title / lead H1. Workspace product name, not
// the org legal name. Dest chips, period control, invoices, contracts,
// and settings company fields stay on their own SoT.

import { WORKSPACE_AGGREGATION_LABEL } from "@/lib/product";

export const AGGREGATION_LEAD_TITLE = WORKSPACE_AGGREGATION_LABEL;
