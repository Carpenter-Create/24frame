// Staff /avails. Layout (Titles-style list without StatusProgressTrack vs
// 3-wide landscape poster grid) is held until Design locks A vs B.
// This module is the empty-shell copy SoT — no list fork, no territory matrix.

export const AVAILS_HREF = "/avails";

export const AVAILS_PAGE = {
  title: "Avails",
} as const;
