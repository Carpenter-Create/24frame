import { AvailsSkeleton } from "@/components/layout/page-skeletons";

// Instant feedback on navigate, and the loading state Next.js prefetches.
export default function Loading() {
  return <AvailsSkeleton />;
}
